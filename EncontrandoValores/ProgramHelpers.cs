internal static class ProgramHelpers
{

    private static int RemoveItem(int[] numeros, int indice)
    {
        int itemRemovido = numeros[indice];
        for (int i = indice; i < numeros.Length)
        {

        }
    }
}